const FamilyMemberCard = ({ familymember }) => (
    <div className="familymember-card">
        <h3>{familymember.name}</h3>
        {/* Adding more fields later */}
    </div>
);

export default FamilyMemberCard;